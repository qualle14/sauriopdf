/**
 * SaurioPDF — lib entry point.
 * Re-exports everything from mod.ts. Use whichever import path you prefer.
 *
 * @example
 * ```ts
 * import { init, PDF, loadLiberationSans } from "./lib/index.ts";
 * ```
 */

export * from "../mod.ts";
